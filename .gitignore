# Credentials — niemals committen (Produktions-Zugangsdaten!)
.env
.env.*
!.env.example

# Editor-Swap-/Backup-Dateien (z. B. Vim ..env.swp — kann .env-Inhalt enthalten!)
*.swp
*.swo
*~
.*.swp

# Python
.venv/
__pycache__/
*.pyc

# Test-/Coverage-Artefakte
.coverage
.coverage.*
.pytest_cache/
htmlcov/

# Selbstsignierte Zertifikate (werden lokal generiert)
*.pem
certs/

# Spike-/Automation-Ausgaben (Screenshots, DOM-Dumps, Traces)
automation/out/

# Laufzeit-Logs (rotierend, server.log*)
logs/

# Serverseitige Laufzeit-Persistenz (Bücher-Reihenfolge/Ausblendung)
data/

# Lokale Testschüler-Liste (echte Schülerdaten — nicht committen!)
tests/test_students.local.json

# macOS Metadata
.DS_Store
._*
